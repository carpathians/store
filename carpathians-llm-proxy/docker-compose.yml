version: "3.7"

services:
  app_proxy:
    environment:
      # Format: <app-id>_<docker-service-name>_1
      APP_HOST: carpathians-llm-proxy_web_1
      APP_PORT: 8788
      # OpenAI /v1 for OpenCode/curl — Bearer key enforced by llm-proxy (LAN API).
      # Without this, Umbrel app_proxy 302s to :2000 and clients cannot connect.
      PROXY_AUTH_WHITELIST: "/v1/*"

  web:
    image: ghcr.io/carpathians/llm-proxy:0.9.20
    # No user: override — entrypoint starts as root, chowns /data, drops to 1000
    init: true
    restart: on-failure
    stop_grace_period: 1m
    environment:
      DATA_DIR: /data
      HOST: "0.0.0.0"
      PORT: "8788"
      # auto → GPU when Umbrel injects /dev/dri (permissions: GPU), else CPU
      LLM_LLAMA_BACKEND: auto
    volumes:
      - ${APP_DATA_DIR}/data:/data
    healthcheck:
      test: ["CMD", "python3", "-c", "import urllib.request; urllib.request.urlopen('http://127.0.0.1:8788/api/health', timeout=4)"]
      interval: 15s
      timeout: 5s
      retries: 5
      start_period: 20s
